{
  "circuit_breaker": {
    "window_size": 10,
    "trip_threshold": 0.2,
    "retryable_trip_count": 3,
    "retryable_trip_rate": 0.5,
    "cooldown_sec": 300,
    "recovery_probe_pct": 0.01,
    "success_to_close": 3,
    "error_burst_window_sec": 120,
    "error_burst_count": 3
  },
  "providers": {
    "codex": {
      "fallback_order": ["opus", "sonnet", "4o-mini"],
      "auth": "page_human",
      "timeout": "retry_then_fallback",
      "rate_limit": "fallback",
      "overload": "fallback",
      "non_retryable": "page_human"
    },
    "opus": {
      "fallback_order": ["codex", "sonnet", "4o-mini"],
      "auth": "page_human",
      "timeout": "retry_then_fallback",
      "rate_limit": "fallback",
      "overload": "fallback",
      "non_retryable": "page_human"
    },
    "sonnet": {
      "fallback_order": ["codex", "opus", "4o-mini"],
      "auth": "page_human",
      "timeout": "retry_then_fallback",
      "rate_limit": "fallback",
      "overload": "fallback",
      "non_retryable": "page_human"
    },
    "4o-mini": {
      "fallback_order": ["codex", "opus", "sonnet"],
      "auth": "page_human",
      "timeout": "retry_then_fallback",
      "rate_limit": "fallback",
      "overload": "fallback",
      "non_retryable": "page_human"
    }
  }
}
