# 2026-02-18

## What Happened
- **Infrastructure path migration cleanup**
  - Updated multiple broken watchdog/service paths as repos moved.
  - Settled on `~/Developer/cortana-external/watchdog` as the canonical watchdog location in `clawd` docs/config.
- **External repo relocation + startup hardening**
  - In `cortana-external`, moved references to `~/cortana-external` and added launchd auto-restart wiring to keep services alive after failures/reboots.

## Key Decisions
- Standardized service location references around `~/Developer/cortana-external` from the assistant workspace perspective, eliminating path drift.
- Added/relied on launchd auto-restart as a reliability baseline instead of manual restarts.

## Notes
- Pattern: rapid follow-up path commits indicate active migration with stale references discovered incrementally.
- This day was mostly operational stabilization, not product feature development.