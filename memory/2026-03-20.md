## Operations / Reliability
- Watchdog `gog` smoke test failure root cause was non-interactive keyring/token access drift; watchdog command was hardened to `timeout 15` + `--no-input`, and `gog` OAuth re-auth was completed on screenshare. Result: watchdog reported recovery and `gog: OK` afterward.
- OpenAI Codex auth failures caused cron jobs to error (`OAuth token refresh failed for openai-codex`). Multiple auth attempts failed due to callback state mismatch and localhost callback port conflict (`127.0.0.1:1455` in use by stuck auth process). After killing stuck listener and completing a fresh auth flow, CLI reported `OpenAI OAuth complete` and updated `~/.openclaw/openclaw.json`.

## Security / Preferences
- User asked about storing bot-account username/password for reauth fallback. Established policy with user: do **not** store raw credentials in chat/repo/memory; use OAuth + keychain/token-based auth, with password manager + quick human approval/screenshare as fallback.
