## Huragok — 2026-03-09
- Fixed repo auto-sync reliability in `~/Developer/cortana` by isolating volatile runtime state from tracked git paths. Moved live cron/runtime state references to ignored `memory/runtime/`, broadened repo-auto-sync preflight to restore known legacy volatile state files by pattern, and tightened ignore rules so generated fitness snapshots stay untracked while docs remain tracked. Opened PR #207 in `hd719/cortana` for remote parity.
- Fixed trading reliability bundle in `~/Developer/cortana-external` and opened PR #81 (`fix: harden trading scanners and Alpaca account targeting`). Durable scanner fix: Dip Buyer now routes through the resilient helper path instead of the legacy path that could stall during market-data fetch. Verified CANSLIM and Dip Buyer return usable output locally on reduced universes.
- Hardened local Alpaca service account targeting in `~/Developer/cortana-external`: added explicit `ALPACA_KEYS_PATH` resolution, `ALPACA_TARGET_ENVIRONMENT` guard, and response metadata exposing environment/keys path/fingerprint so the service stops silently trusting the wrong account context. Important blocker discovered: host credential file `~/Desktop/services/alpaca_keys.json` still contains paper Alpaca keys, so `localhost:3033/alpaca/portfolio` cannot show live holdings until live creds are supplied.
- Cleaned up user-facing CANSLIM / Dip Buyer alert formatting and opened PR #82 in `hd719/cortana-external`. Suppressed warning spam from yfinance/Pandas in user-visible output, removed duplicate blocker/sample clutter, and shifted alerts to concise market/summary/top-names messaging while preserving scanner logic.
- Session model changed successfully from `openai-codex/gpt-5.3-codex` to `openai-codex/gpt-5.4` after user requested the switch on 2026-03-09.
- User preference: keep replies concise by default; only go deeper when explicitly asked.
- Telegram security was tightened without breaking agent comms: group policy moved to allowlist with Hamel's sender ID allowed; specialist routing Researcher/Monitor/Oracle/Huragok was verified live after the change.
- OpenClaw install state on host: CLI is `2026.3.8`, but the running gateway previously reported app `2026.3.7`; ACP config was added (`acp.enabled=true`, backend `acpx`, default agent `codex`, allowed agents `codex`/`claude`, acpx plugin enabled), but ACP smoke test still failed with "ACP runtime backend is not configured" before a full gateway service restart could be verified.
