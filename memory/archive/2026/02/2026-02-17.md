# 2026-02-17 (Tuesday)

## Calendar Reminder Fix
- Fixed calendar reminder cron firing too early (390m before event instead of T-60m/T-10m)
- Root cause: cron prompt didn't specify time windows, so it would fire reminders whenever it saw upcoming events
- Added explicit time windows to the cron prompt so reminders only fire within the correct windows (T-60m and T-10m)

## Open-Meteo Weather Fallback
- Added Open-Meteo as fallback weather source when wttr.in is down
- Created `skills/weather/SKILL.md` with primary + fallback documentation
- Updated immune system cron to try Open-Meteo before ignoring weather outages
- Updated proprioception cron to try Open-Meteo fallback for weather checks
- Added weather section to TOOLS.md

## Preferences
- Added 🫀 heartbeat tag preference — heartbeat-triggered messages to user should be prefixed with 🫀

## Cron Fix: Daily Upgrade Protocol
- **Problem 1**: Cron skipping days (Feb 16 → Feb 18, skipped Feb 17). Deleted 30 stale session files (total ~5MB) that were accumulating context and causing issues.
- **Problem 2**: Sub-agent dedup logic was broken — sub-agent would read yesterday's upgrade file and think today was already done. Added explicit instructions to check TODAY's date file specifically and always generate fresh proposals.
- **Problem 3**: Delivery was missing channel/to fields — only had `{"mode": "announce"}`. Added explicit Telegram delivery to `8171372724`.
- All session files referencing the cron job ID were purged to ensure clean slate.
