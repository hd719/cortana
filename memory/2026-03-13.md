- Verified ACPX failures were caused by restrictive runtime config for non-interactive coding runs. Live OpenClaw config was patched so `plugins.entries.acpx.config.permissionMode` changed from `approve-reads` to `approve-all`; this restored Codex ACP write/exec capability. Repo audit trail logged in cortana PR #282.
- Hamel said to use `/Users/hd/.acpx` as the first place to debug ACPX state/issues in the future.
- After every successful merge, Hamel wants all ACP sessions purged/cleaned up. This is now an explicit cleanup preference.
- Verified Telegram ACP thread spawn is still not actually supported in the running OpenClaw 2026.3.12 runtime despite docs/config suggesting support; live smoke test error: `Thread bindings do not support ACP thread spawn for telegram.`
- Live gateway config was patched to set explicit trusted plugin allowlist `plugins.allow = ["telegram", "memory-core", "acpx"]`.
- Investigated unified CANSLIM + Dip Buyer cron failures. Real bottleneck is not only timeout values: the full 120-symbol scans are too heavy in their current monolithic form. Direct host proof showed CANSLIM with correct backtester venv succeeds at smaller universe sizes (20 symbols finished in ~69s) while full 120 drifts into timeout territory.
- Hamel explicitly wants the trading scans to keep the full 120-symbol universe. Preferred fix direction is chunked execution/merge of full 120 coverage rather than permanently shrinking the universe.
