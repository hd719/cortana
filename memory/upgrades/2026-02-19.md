# Upgrade Log - 2026-02-19

## Daily Upgrade Proposal

**Git:** Committed 2 files (daily auto-commit)

**Yesterday's gap:** Tonal auth failures keep requiring manual intervention. The feedback log says it plainly: "Self-heal rules in docs are not enough — must be implemented IN THE SERVICE CODE." The fitness service itself doesn't auto-delete stale tokens on auth failure, so every time Tonal auth expires, Cortana has to manually detect and fix it instead of the service self-recovering.

**Proposed fix:** Patch the fitness service (`~/Developer/cortana-external/fitness/`) to auto-handle Tonal auth failures:
1. On 401/403 from Tonal API → automatically delete cached token file
2. On next request → trigger re-auth flow automatically
3. Log the self-heal to `cortana_events` so we have visibility
4. Remove the manual playbook from MEMORY.md since it'll be baked into the service

**Effort:** Medium (1-2 hours — need to read the fitness service code, find auth flow, add retry logic)

**Autonomy gain:**
- Eliminates recurring manual Tonal auth fixes
- Fitness morning briefs stop breaking when tokens expire
- Moves self-heal from "Cortana playbook" to "service-level resilience"
- Pattern fix: infrastructure should self-heal, not rely on AI operator intervention

## Status
- **Proposed:** 2026-02-19 10:00 AM ET
- **Status:** Awaiting approval
